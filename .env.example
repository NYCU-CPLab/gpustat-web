# Authentication credentials
CADDY_AUTH_USERNAME=admin

# This is the hash for "password123"
# Generate your own with: docker run --rm caddy:2-alpine caddy hash-password
CADDY_AUTH_PASSWORD=$2a$14$0xVbI0hxgZrq/Lfgb.hVTOWpZ6vF3uCXA9lk5FQJH2L9e.iYVy.4W

# Site address for Caddy
# Use your domain for automatic HTTPS, or use :80 for localhost without a domain
SITE_ADDRESS=gpustat.example.com

# GPU hosts configuration
GPU_HOSTS=user@a.example.com user@b.example.com
# GPU_HOSTS_OPTION=--no-verify-host

# Web server ports (optional)
# WEB_PORT=80
# SSL_PORT=443
